@model Sensors.Models.Sensors.CreateSensorDTO

@{
    ViewData["Title"] = "Create";
}

<h1>Create Sensor for Zone: @ViewBag.Zone.Name</h1>
<p>@ViewBag.Zone.Description</p>

<h2>Create</h2>

<h4>Sensor</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" asp-route-zoneId="@ViewBag.Zone.Id">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" /> 
                <a asp-action="Index" asp-route-zoneId="@ViewBag.Zone.Id" class="btn btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</div>

@*<div>
    
</div>
*@

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
